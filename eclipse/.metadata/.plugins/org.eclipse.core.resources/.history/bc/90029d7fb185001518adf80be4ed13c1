package com.ashindigo.utils;

import java.util.ArrayList;

import cpw.mods.fml.common.registry.GameRegistry;
import net.minecraft.creativetab.CreativeTabs;
import net.minecraft.entity.Entity;
import net.minecraft.item.Item;
import net.minecraft.item.ItemArmor;
import net.minecraft.item.ItemStack;

/**
 * A class used to automatically make an Armor set and register it;
 * @author 19jasonides_a
 */
public class UtilsArmor extends ItemArmor {

	static ArrayList listitem = new ArrayList();
	static int runtime = 0;
	public static ArrayList armoritemlists = new ArrayList();
	public String MODID;
	public String textureName;
	
	public UtilsArmor(ArmorMaterial p_i45325_1_, int p_i45325_2_, int p_i45325_3_) {
		super(p_i45325_1_, p_i45325_2_, p_i45325_3_);
		// TODO Auto-generated constructor stub
	}
	
	public UtilsArmor(String unlocalizedName, ArmorMaterial material, int type, String modid) {
	    super(material, 0, type);
	    this.textureName = unlocalizedName;
	    this.setUnlocalizedName(unlocalizedName);
	    this.setTextureName(modid + ":" + unlocalizedName);
	    setCreativeTab(CreativeTabs.tabCombat);
	    //this.MODID = modid;
	    System.out.println(unlocalizedName);
	    System.out.println(listitem);
	    System.out.println(armoritemlists);
	    listitem.add(unlocalizedName);
	}

	@Override
	public String getArmorTexture(ItemStack stack, Entity entity, int slot, String type)
	{
	    return MODID + ":armor/" + this.textureName + "_" + (this.armorType == 2 ? "2" : "1") + ".png";
	}
	
	/**
	 * Method that starts the automatic registration of Armor Items.
	 * Call in init method
	 * See:{@link UtilsMain.init}
	 */
	public static void register() {
		try{
		while(runtime < listitem.size()){
		GameRegistry.registerItem((Item) armoritemlists.get(runtime), ("indigoapiarmorwow" + runtime));
		runtime++;
		}
		catch(IndexOutOfBoundsException){
			
		}
		}
	}
}
